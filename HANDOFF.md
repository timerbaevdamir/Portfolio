# Состояние на 19 августа 2026

Снимок: что сделано, что открыто, что уже проверено — чтобы не проверять
заново. Постоянные правила проекта — в [AGENTS.md](AGENTS.md).

- Репозиторий: `github.com/timerbaevdamir/Portfolio`, ветка `main`
- Деплой: Vercel, `portfolio-svdq.vercel.app`
- Проверки чистые: `typecheck`, `lint`, `build`

## Что стоит сейчас

**Витрина.** Слева фиксированная колонка, которая не скроллится: круглое фото,
имя, роль, «о себе», опыт (пустой, см. ниже), ссылки. Справа скроллится сетка
плиток по две в ряд. На телефоне колонка убирается в панель, которая
открывается кнопкой «Обо мне» из верхней полоски.

Плитка — картинка с подписью, **без рамки**: превью само несёт край, коробка
вокруг него была второй границей вокруг одного объекта. Года на плитках нет,
он только внутри кейса.

**Кейс.** Рейл (✕ домой, тумблер описания, ↑ n/N ↓), колонка описания
(вводные → факты таблицей → нумерованные задачи → разделы → «Детали» со стеком
и ссылкой на код) и сцена. Верхнего хедера **нет** — он повторял то, что и так
на экране, и отбирал высоту у телефона, которому её и не хватает. Дорога домой
— ✕ в рейле.

**Три проекта:** `job-board` (живой, телефон + десктоп), `ui-agent` (живой,
только десктоп), `coffee-map` (обложкой, см. ниже).

## Открытое

### 1. Опыт в боковой колонке пуст

`SITE.experience` — пустой массив, секция не рендерится, пока в нём ничего нет.
Оставлено пустым намеренно: работодатель — факт о человеке, и он появляется на
публичной странице, только если человек его написал, а не потому что его можно
угадать по почте в гите. **Заполнить должен автор.**

### 2. Плитка Кофейной карты выглядит сломанной

Проект больше не встраивается, поэтому на витрине его плитка — тёмный
прямоугольник с надписью `coffeemap.ru` рядом с двумя живыми превью. Честно,
но читается как «не загрузилось».

Закрывается скриншотом: API поднимается примерно раз из трёх, можно поймать
рабочий момент, снять карту и положить файл в `public/`. **Предложено, не
сделано** — ждёт решения автора.

Если делать: поля `poster` в модели больше нет — оно ничего не рендерило и было
убрано. Возвращать вместе с отрисовкой и в `Thumb`, и в `Cover`.

### 3. API Кофейной карты падает — диагностика проведена, чинить не здесь

`coffeemap.ru/api/coffee-shops` отдаёт 500 с `{"error":"TypeError: fetch
failed"}`. Портфолио ни при чём: при прямом открытии сайта то же самое.

Уже проверено, **перепроверять не нужно**:

| Что | Результат |
|---|---|
| Проект Supabase `zofruwsqkjnperxltgrt` | `ACTIVE_HEALTHY` |
| REST снаружи | 200 за 2.1 с, данные отдаёт |
| Вью `api_coffeeshops_full_view` | существует |
| Сам запрос в БД | 73 мс, 539 кофеен |
| Три запроса подряд к сайту | 200 за 20.7 с, потом 500 за 10.6 с, 500 за 10.6 с |

Ключевое: **иногда проходит**. Значит переменные окружения целы — неверный URL
или ключ падали бы всегда. Повторяющиеся 10.6 с — дефолтный `connectTimeout`
undici, то есть соединение не открывается вовсе. Сайт живёт на Yandex.Cloud
(RU, 93.77.183.4), Supabase за зарубежным CDN; дальше это догадка, но
связность, судя по цифрам, плавает.

Чинится в репозитории `~/Documents/coffee-map-app`, в
`app/api/coffee-shops/route.ts`: сейчас первая же неудача уходит в `catch` и
возвращает 500, ретрая нет, а кэш живёт в памяти процесса и пуст после
рестарта. Один ретрай плюс отдача устаревшего кэша при сбое закрыли бы ровно
этот сценарий.

Когда устаканится — вернуть `embed: true` у `coffee-map`. Вьюпорты для него
оставлены на месте, фрейм вернётся целиком.

### 4. Кейсы-видео и гифки — отложено автором

Часть будущих работ это не сайты, а записи экрана. Договорённость: **займёмся
потом.** Рекомендация в силе — хранить в `public/`, брать MP4, а не GIF, и
описать медиа размеченным объединением вместо флага:

```ts
type Media = { kind: "live" } | { kind: "video"; src: string } | { kind: "link" }
```

Открытый вопрос, который надо будет задать: одно видео на кейс или своё на
каждый раздел.

### 5. Мелочи

- Контакты — только GitHub. Почта или телеграм не опубликованы; в `site.ts`
  стоит закомментированное место под них.
- Джоб-борд задеплоен как `portfolio-mu-black-10.vercel.app` — имя проекта на
  Vercel осталось от шаблона и путается с самим портфолио. Переименовать можно,
  автор пока не просил.

## Как работать с автором

- **Не гонять браузер ради проверки вёрстки.** Автор просил этого не делать без
  прямой просьбы: сделать, проверить типами и сборкой, отдать. Открывать
  браузер, чтобы воспроизвести сообщённый баг или посмотреть референс, — можно.
- Коммитить и пушить по ходу дела, автор этого ждёт.
